version: '3.3'

services:

  nginx:
    build:
      context: .
      dockerfile: Dockerfile-test-nginx
      args:
        - BASE_IMAGE=${IMAGE_NAME:-teslagov/jwt-nginx}

  runner:
    build:
      context: .
      dockerfile: Dockerfile-test-runner
    depends_on:
      - nginx-test