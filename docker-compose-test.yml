version: '3.3'

services:

  nginx:
    build:
      context: .
      dockerfile: Dockerfile-test-nginx

  runner:
    build:
      context: .
      dockerfile: Dockerfile-test-runner
    depends_on:
      - nginx-test